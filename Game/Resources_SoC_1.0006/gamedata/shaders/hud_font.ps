#include "common.h"

float4 main(p_TL I) : SV_Target
{
    if (font_params.x > 1.5f)
        return float4(I.Color.rgb, s_base.Sample(smp_base, I.Tex0).r * I.Color.a);

    float4 r = s_base.Sample(smp_base, I.Tex0);
    r.a *= I.Color.a;

    return r;
}
